# Secrets / config locale
.env
.env.*
!.env.example
!.env.*.example

# Backups — locaux/VPS uniquement, jamais en git
backups/
*.bak
*.bak*
# Backups KB V1 produits par v2/scripts/migrate_kb_v1_v2.py (Sprint 5.1)
# Le dossier grossit à chaque rerun ; les originaux sont déjà sous git.
data/v1_backup/
# Backups KB V2 produits par v2/scripts/sprint53_merge_drafts.py (Sprint 5.3)
data/v2_backup/

# Logs
logs/
*.log

# Virtualenvs
.venv/
.venv_scraper/
venv/
env/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Couverture pytest (artefact local — régénéré par CI à chaque run)
.coverage
.coverage.*
htmlcov/
coverage.xml

# Données de run (dupliquées sur VPS — pas en git)
data/rendez_vous.json
data/appels_15min.json
data/emails_juriste.json
data/interactions.jsonl

# KB volumineuses (1.4 GB de PDF/PPTX) — montées via volume ./data:/app/data,
# déployées via rsync séparément, pas à pousser sur GitHub.
data/alisfa_docs/
data/cpnef_docs/
data/alisfa_public/
data/.trash_*/
fiches_pratiques/

# Artefacts d'éval RAG
tests/rag_eval_report_*.json

# Éditeurs
.vscode/
.idea/
.DS_Store
*.swp

# Caches embeddings Voyage (Sprint 5.2-stack)
data/v2/_embeddings_*.npz
